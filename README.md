# hideyasu-react-native

## railsプロジェクトの作成
````
$ docker-compose run --rm rails rails new . --force --skip-bundle --skip-test --api
````

## react-nativeの動かし方
````
$ cd react-native
$ yarn start
````
